#include <stdio.h>

/** main - Entry point of program
 * prints lowercase and upper cases of the alphabet
 * return: (0) Success
 */
int main(void)
{

char ch;

for (ch = 'a' ; ch <= 'z' ; '\n' ; ch++)
{
putchar(ch);
}

return (0);
}

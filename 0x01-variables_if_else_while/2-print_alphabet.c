#include <stdio.h>

/**
 * main - Entry point of program
 * prints the alphabet in lowercase
 * Return: Always (0) Success
 */

#include <stdio.h>

int main ()
{

char ch;

for(ch = 'A' ; ch <= 'Z' ; ch++)
{
putchar(ch);
}
putchar('\n'):

return(0);

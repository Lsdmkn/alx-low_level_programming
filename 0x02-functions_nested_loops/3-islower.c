#include "main.h"

/**
 * _islower - checks for lowercase character
 * @ch: an agrument for the character
 * Return: 0 and 1 based on conditions
 */
int _islower(int ch)
{
	return (ch >= 'a' && ch <= 'z');
}
